<Project Sdk="Microsoft.NET.Sdk">
  <Import Project="$(MSBuildExtensionsPath)\$(MSBuildToolsVersion)\Microsoft.Common.props" Condition="Exists('$(MSBuildExtensionsPath)\$(MSBuildToolsVersion)\Microsoft.Common.props')" />
  <PropertyGroup>
    <Configuration Condition=" '$(Configuration)' == '' ">Debug</Configuration>
    <Platform Condition=" '$(Platform)' == '' ">AnyCPU</Platform>
    <ProjectGuid>{41D20001-6ADD-4B78-9EEA-F6BE3ED30820}</ProjectGuid>
    <OutputType>Library</OutputType>
    <AppDesignerFolder>Properties</AppDesignerFolder>
    <RootNamespace>DynamoPilot.App</RootNamespace>
    <AssemblyName>DynamoPilot.App.ext2</AssemblyName>
    <TargetFramework>net481</TargetFramework>
    <FileAlignment>512</FileAlignment>
    <Deterministic>true</Deterministic>
    <AssemblyProduct>DynamoPilot.App</AssemblyProduct>
    <Product>DynamoPilot.App</Product>
    <Description>DynamoPilot.App</Description>
    <AssemblyVersion>0.0.0.1</AssemblyVersion>
    <UseWPF>true</UseWPF>
    <LangVersion>10.0</LangVersion>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|AnyCPU'">
    <DebugSymbols>true</DebugSymbols>
    <DebugType>full</DebugType>
    <Optimize>false</Optimize>
    <OutputPath>bin\Debug\</OutputPath>
    <DefineConstants>DEBUG;TRACE</DefineConstants>
    <WarningLevel>4</WarningLevel>
    <StartAction>Program</StartAction>
    <StartProgram>C:\Program Files\ASCON\Pilot-ICE Enterprise\Ascon.Pilot.PilotEnterprise.exe</StartProgram>
  </PropertyGroup>

  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|AnyCPU' ">
    <DebugType>pdbonly</DebugType>
    <Optimize>true</Optimize>
    <OutputPath>..\bin\Release\</OutputPath>
    <DefineConstants>TRACE</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>4</WarningLevel>
  </PropertyGroup>

  <!-- Пути к папкам для дебага -->
  <PropertyGroup>
    <AppData>$(USERPROFILE)\AppData\Local</AppData>
    <UserDirectoryPathBIM>$(AppData)\ASCON\Pilot-BIM\Development\$(AssemblyProduct)</UserDirectoryPathBIM>
    <UserDirectoryPathICE>$(AppData)\ASCON\Pilot-ICE Enterprise\Development\$(AssemblyProduct)</UserDirectoryPathICE>
    <BaseOutputPath>..\bin\</BaseOutputPath>
  </PropertyGroup>

  <!-- Копирование файлов в папки для дебага -->
  <Target Name="CopyToDebugDirectory" AfterTargets="Build" Condition="'$(Configuration)' == 'Debug'">
    <MakeDir Directories="$(UserDirectoryPathBIM)" Condition="!Exists('$(UserDirectoryPathBIM)')" />
    <ItemGroup>
      <FilesToCopy Include="$(OutputPath)**\*.*" />
    </ItemGroup>
    <Copy SourceFiles="@(FilesToCopy)" DestinationFolder="$(UserDirectoryPathBIM)" />

    <MakeDir Directories="$(UserDirectoryPathICE)" Condition="!Exists('$(UserDirectoryPathICE)')" />
    <MakeDir Directories="$(UserDirectoryPathBIM)\packages\DynamoPilot\bin" Condition="!Exists('$(UserDirectoryPathBIM)\packages\DynamoPilot\bin')" />
    <MakeDir Directories="$(UserDirectoryPathICE)\packages\DynamoPilot\bin" Condition="!Exists('$(UserDirectoryPathICE)\packages\DynamoPilot\bin')" />
    <ItemGroup>
      <FilesToCopy Include="$(OutputPath)**\*.*" />
    </ItemGroup>
    <Copy SourceFiles="@(FilesToCopy)" DestinationFolder="$(UserDirectoryPathICE)" />

    <!-- Копируем DynamoCoreRuntime2.16.4 в корень приложения -->
    <ItemGroup>
      <DynamoRuntimeFiles Include="..\..\lib\DynamoCoreRuntime2.16.4\**\*.*" />
    </ItemGroup>
    <Copy SourceFiles="@(DynamoRuntimeFiles)" 
          DestinationFiles="@(DynamoRuntimeFiles->'$(UserDirectoryPathBIM)\%(RecursiveDir)%(Filename)%(Extension)')" 
          SkipUnchangedFiles="true" />
    <Copy SourceFiles="@(DynamoRuntimeFiles)" 
          DestinationFiles="@(DynamoRuntimeFiles->'$(UserDirectoryPathICE)\%(RecursiveDir)%(Filename)%(Extension)')" 
          SkipUnchangedFiles="true" />

    <!-- Копируем PilotNodes.xml из Zero проекта -->
    <ItemGroup>
      <PilotNodesFiles Include="..\DynamoPilot.Zero\bin\Debug\net481\PilotNodes.xml" />
    </ItemGroup>
    <Copy SourceFiles="@(PilotNodesFiles)" 
          DestinationFolder="$(UserDirectoryPathBIM)\packages\DynamoPilot\bin" 
          SkipUnchangedFiles="true" />
    <Copy SourceFiles="@(PilotNodesFiles)" 
          DestinationFolder="$(UserDirectoryPathICE)\packages\DynamoPilot\bin" 
          SkipUnchangedFiles="true" />

    <!-- Копируем pkg.json из Nodes проекта -->
    <ItemGroup>
      <PkgJsonFile Include="..\DynamoPilot.Nodes\pkg.json" />
    </ItemGroup>
    <Copy SourceFiles="@(PkgJsonFile)" 
          DestinationFolder="$(UserDirectoryPathBIM)\packages\DynamoPilot" 
          SkipUnchangedFiles="true" />
    <Copy SourceFiles="@(PkgJsonFile)" 
          DestinationFolder="$(UserDirectoryPathICE)\packages\DynamoPilot" 
          SkipUnchangedFiles="true" />
  </Target>

  <!-- Создаем архив на релизе -->
  <Target Name="CreateZip" AfterTargets="Build" Condition="'$(Configuration)' == 'Release'">
    <!-- Создаем временную папку для архива -->
    <PropertyGroup>
      <TempArchiveDir>$(OutputPath)TempArchive</TempArchiveDir>
    </PropertyGroup>
    
    <!-- Очищаем временную папку если она существует -->
    <RemoveDir Directories="$(TempArchiveDir)" Condition="Exists('$(TempArchiveDir)')" />
    <MakeDir Directories="$(TempArchiveDir)" />
    
    <!-- Копируем основные файлы App (без pdb, xml, zip архивов и временных папок) -->
    <ItemGroup>
      <AppFiles Include="$(OutputPath)**\*.*" Exclude="$(OutputPath)**\*.pdb;$(OutputPath)**\*.xml;$(OutputPath)**\*.zip;$(OutputPath)TempArchive\**\*.*;$(OutputPath)TestExtract\**\*.*" />
    </ItemGroup>
    <Copy SourceFiles="@(AppFiles)" 
          DestinationFiles="@(AppFiles->'$(TempArchiveDir)\%(RecursiveDir)%(Filename)%(Extension)')" />
    
    <!-- Копируем все файлы DynamoCoreRuntime2.16.4 без исключений -->
    <ItemGroup>
      <DynamoRuntimeAllFiles Include="..\..\lib\DynamoCoreRuntime2.16.4\**\*.*" />
    </ItemGroup>
    <Copy SourceFiles="@(DynamoRuntimeAllFiles)" 
          DestinationFiles="@(DynamoRuntimeAllFiles->'$(TempArchiveDir)\%(RecursiveDir)%(Filename)%(Extension)')" />
    
    <!-- Создаем папку packages и копируем файлы -->
    <MakeDir Directories="$(TempArchiveDir)\packages\DynamoPilot\bin" />
    
    <!-- Копируем PilotNodes.xml и все библиотеки из Zero проекта -->
    <ItemGroup>
      <ZeroFiles Include="..\DynamoPilot.Zero\bin\Release\net481\PilotNodes.xml;..\DynamoPilot.Zero\bin\Release\net481\PilotNodes.dll;..\DynamoPilot.Zero\bin\Release\net481\PilotNodes.pdb" />
    </ItemGroup>
    <Copy SourceFiles="@(ZeroFiles)" 
          DestinationFolder="$(TempArchiveDir)\packages\DynamoPilot\bin" />
    
    <!-- Копируем библиотеки из Nodes проекта -->
    <ItemGroup>
      <NodesFiles Include="..\DynamoPilot.Nodes\bin\Release\net481\DynamoPilot.Nodes.dll;..\DynamoPilot.Nodes\bin\Release\net481\DynamoPilot.Nodes.pdb" />
    </ItemGroup>
    <Copy SourceFiles="@(NodesFiles)" 
          DestinationFolder="$(TempArchiveDir)\packages\DynamoPilot\bin" />
    
    <!-- Копируем pkg.json -->
    <ItemGroup>
      <PkgJsonFile Include="..\DynamoPilot.Nodes\pkg.json" />
    </ItemGroup>
    <Copy SourceFiles="@(PkgJsonFile)" 
          DestinationFolder="$(TempArchiveDir)\packages\DynamoPilot" />
    
    <!-- Создаем архив из временной папки -->
    <Message Text="Создаем архив $(AssemblyProduct).zip..." Importance="high" />
    <Exec Command="powershell -Command &quot;Compress-Archive -Path '$(TempArchiveDir)\*' -DestinationPath '$(OutputPath)$(AssemblyProduct).zip' -Force&quot;" />
    <Message Text="Архив создан успешно!" Importance="high" />
    
    <!-- Удаляем временную папку -->
    <RemoveDir Directories="$(TempArchiveDir)" />
  </Target>
  
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|AnyCPU' ">
    <DebugType>pdbonly</DebugType>
    <Optimize>true</Optimize>
    <OutputPath>bin\Release\</OutputPath>
    <DefineConstants>TRACE</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>4</WarningLevel>
  </PropertyGroup>
  
  <ItemGroup>
    <PackageReference Include="Ascon.Pilot.SDK" Version="25.9.0" />
    <PackageReference Include="DynamoVisualProgramming.Core" Version="2.16.4.10216" />
    <PackageReference Include="DynamoVisualProgramming.WpfUILibrary" Version="2.16.4.10216" />
    <PackageReference Include="HelixToolkit.Wpf" Version="2.27.0" />
    <PackageReference Include="Microsoft.Web.WebView2" Version="1.0.3351.48" />
    <PackageReference Include="RxExt" Version="1.0.2" />
    <PackageReference Include="SharpDX" Version="4.2.0" />
    <PackageReference Include="SharpDX.Mathematics" Version="4.2.0" />
  </ItemGroup>
  
  <ItemGroup>
    <ProjectReference Include="..\DynamoPilot.Data\DynamoPilot.Data.csproj" />
  </ItemGroup>

  <ItemGroup>
    <Reference Include="CoreNodeModels">
      <HintPath>..\..\..\..\..\AppData\Local\ASCON\Pilot-ICE Enterprise\Development\DynamoPilot.App\CoreNodeModels.dll</HintPath>
    </Reference>
    <Reference Include="CoreNodeModels.resources">
      <HintPath>..\..\lib\CoreNodeModels.resources.dll</HintPath>
    </Reference>
    <Reference Include="CoreNodeModelsWpf">
      <HintPath>..\..\..\..\..\AppData\Local\ASCON\Pilot-ICE Enterprise\Development\DynamoPilot.App\CoreNodeModelsWpf.dll</HintPath>
    </Reference>
    <Reference Include="Greg">
      <HintPath>..\..\lib\Greg.dll</HintPath>
    </Reference>
    <Reference Include="System.ComponentModel.Composition" />
  </ItemGroup>
  

</Project>